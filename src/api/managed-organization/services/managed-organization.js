'use strict';

/**
 * managed-organization service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::managed-organization.managed-organization');
